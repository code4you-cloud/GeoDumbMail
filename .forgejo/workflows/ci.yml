name: Test CI Pipeline
on:
  push:
    branches: [ develop, main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: docker
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run a simple test
        run: echo "CI funziona! Runner attivo."
