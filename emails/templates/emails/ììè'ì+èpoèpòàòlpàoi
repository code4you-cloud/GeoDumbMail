<!DOCTYPE html>
<html>
<head>
    <title>Email Data</title>
</head>
<body>
    <!-- Blocca per mostrare i messaggi di Django -->
    {% if messages %}
    <ul class="messages">
        {% for message in messages %}
        <li class="{% if message.tags %}{{ message.tags }}{% else %}info{% endif %}">
            {{ message }}
        </li>
        {% endfor %}
    </ul>
    {% endif %}

    <h1>Elenco dei dati delle email</h1>
    <table border="1">
        <tr>
            <th>Latitude</th>
            <th>Longitude</th>
            <th>City</th>
            <th>Address</th>
            <th>ImageID</th>
            <th>ImageTime</th>
            <th>ImageURL</th>
            <th>Image Filename</th>
        </tr>
        {% for email in emails %}
        <tr>
            <td>{{ email.latitude }}</td>
            <td>{{ email.longitude }}</td>
            <td>{{ email.city }}</td>
            <td>{{ email.address }}</td>
            <td>{{ email.image_id }}</td>
            <td>{{ email.image_time }}</td>
            <td>{{ email.image_url }}</td>
            <td>{{ email.image_file }}</td>
            <td>
                {% if email.latitude and email.longitude %}
                <a href="https://maps.citylog.cloud/sample_leaflet.html?lat={{ email.latitude }}&lon={{ email.longitude }}&zoom=15&address={{ email.address }}" target="_blank">Vedi sulla mappa</a>

                {% else %}
                Nessuna posizione disponibile
                {% endif %}
            </td>

        </tr>
        {% endfor %}
    </table>
</body>
</html>

